import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  conatiner: {
    padding: 10,
  },
  heading: {
    fontSize: 28,
    fontWeight: '900',
  },
  textInput: {
    marginVertical: 20,
  },
});

export default styles;
